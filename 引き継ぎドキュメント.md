# WordPressプラグイン「社員情報管理システム」引き継ぎドキュメント

## プロジェクト概要

WordPressの管理画面で動作する社員情報管理プラグイン。  
社員の基本情報・学歴・職歴・資格・扶養者・保険情報などをDBで一元管理する。

- **プラグイン名：** 社員情報管理システム
- **プラグインslug：** employee-manager
- **バージョン：** 1.0.0
- **最新ZIP：** `employee-manager.zip`（随時上書き更新）

---

## ファイル構成（全15ファイル）

```
employee-manager/
├── employee-manager.php              # メインファイル・公開API定義
├── uninstall.php                     # アンインストール時の全テーブル削除
├── includes/
│   ├── class-db-install.php          # DB初期化（12テーブル作成）
│   ├── class-employee.php            # 社員CRUD・Ajax処理
│   ├── class-master.php              # マスタデータCRUD・Ajax処理
│   ├── class-csv-export.php          # CSV出力・テンプレート保存
│   └── class-csv-import.php          # CSVインポート（3種対応）
└── admin/
    ├── class-admin-menu.php          # メニュー登録・アセット読み込み・Ajax登録
    ├── assets/
    │   ├── admin.js                  # フロントエンド全処理（jQuery）
    │   └── admin.css                 # 管理画面スタイル
    └── views/
        ├── employee-list.php         # 社員一覧ページ
        ├── employee-form.php         # 社員登録・編集フォーム（5ステップ）
        ├── master.php                # マスタ管理ページ
        ├── csv.php                   # CSV出力ページ
        └── employee-import.php      # CSVインポートページ（3タブ）
```

---

## データベース設計（12テーブル）

| テーブル名 | 内容 |
|---|---|
| `wp_emp_master` | 社員基本情報（1:1） |
| `wp_mst_affiliation` | 所属マスタ |
| `wp_mst_department` | 部署マスタ |
| `wp_mst_position` | 役職マスタ |
| `wp_mst_job_type` | 職種マスタ |
| `wp_emp_insurance` | 保険情報（1:1） |
| `wp_emp_retirement` | 退職情報（1:1） |
| `wp_emp_education` | 学歴（1:多） |
| `wp_emp_career` | 職歴（1:多） |
| `wp_emp_qualification` | 資格（1:多） |
| `wp_emp_dependent` | 扶養者（1:多） |
| `wp_emp_csv_template` | CSV出力テンプレート保存 |

---

## 管理画面メニュー構成

- 社員情報管理（トップメニュー）
  - 社員一覧
  - 新規社員登録
  - マスタ管理
  - CSV出力
  - CSVインポート

---

## 社員登録フォーム（5ステップ）

1. 基本情報（氏名・生年月日・性別など）
2. 所属・役職（所属・部署・役職・職種）
3. 連絡先（住所・電話・緊急連絡先）
4. 経歴・資格・扶養者（動的行追加・削除）
5. 保険・退職・確認画面

---

## CSVインポート機能（3種）

| タブ | ひな形ファイル名 | 対象テーブル |
|---|---|---|
| ① 基本情報 | `①基本情報インポートひな形.csv` | emp_master / 保険 / 退職 |
| ② 経歴・資格 | `②経歴・資格インポートひな形.csv` | 学歴 / 職歴 / 資格 |
| ③ 扶養者 | `③扶養者インポートひな形.csv` | emp_dependent |

**インポート順序：** ① → ② → ③（②③は社員コードで紐付けるため①が先）

**②経歴・資格の「種別」列：** `学歴` / `職歴` / `資格` のいずれかを入力

**日付形式：** `2020-01-01` / `2020/01/01` / `2020-1-1` / `2020/1/1` すべて対応

**重複処理（①基本情報のみ）：**
- スキップ：既存社員コードは無視して新規のみ登録
- 上書き更新：既存社員コードのデータをCSVで上書き

**②③の注意点：** 現状は常に追記。同じCSVを2回インポートするとデータが重複する。

---

## 公開API（他プラグインから呼び出し可能）

```php
emp_get_employee( $id )              // IDで1件取得
emp_get_employee_by_code( $code )    // 社員コードで1件取得
emp_get_employees( $args )           // 一覧取得（検索・ページング対応）
emp_save_employee( $data, $id )      // 保存（新規/更新）
emp_delete_employee( $id )           // 削除
emp_get_master_list( $type )         // マスタ一覧取得
emp_get_active_employees()           // 在籍中社員のみ取得
```

---

## AJAXアクション一覧

| アクション | 処理 |
|---|---|
| `emp_employee_save` | 社員保存（新規/更新） |
| `emp_employee_get` | 社員1件取得 |
| `emp_employee_list` | 社員一覧取得 |
| `emp_employee_delete` | 社員削除 |
| `emp_employee_toggle_active` | 在籍/退職切り替え |
| `emp_master_get_list` | マスタ一覧取得 |
| `emp_master_save` | マスタ保存 |
| `emp_master_delete` | マスタ削除 |
| `emp_master_toggle_active` | マスタ有効/無効切り替え |
| `emp_csv_export` | CSV出力 |
| `emp_csv_template_save` | CSVテンプレート保存 |
| `emp_csv_template_load` | CSVテンプレート読み込み |
| `emp_csv_template_download` | ひな形CSVダウンロード |
| `emp_csv_import` | CSVインポート実行 |

---

## これまでに修正・実装した主な内容

### UIの修正
- 社員一覧：行クリックではなく編集ボタンのみで編集画面に遷移
- フォームステップタブをクリックで移動可能に
- ナビゲーションボタン配置変更（登録/更新ボタンを左、前へ/次へを右）
- CSV出力ページの右側カードにpadding追加

### バグ修正
- CSSがWordPressデフォルトスタイルに負ける問題（`.emp-wrap`でスコープ化）
- `enqueue_assets()`のhookパラメータ問題（`$_GET['page']`で判定に変更）
- `fillForm()`が学歴・職歴・資格・扶養者の動的行を復元できない問題
- `collectFormData()`が動的行（`name="educations[school_name][]"`形式）を収集しない問題
- `save_children()`に`wp_unslash()`が抜けていた問題
- `save_children()`で`created_at`/`updated_at`が未セットだった問題
- `parse_date()`が`2020/01/01`や`2020/1/1`を受け付けない問題

### CSVインポート機能の拡張
- 基本情報のみ対応 → 3種（基本情報・経歴資格・扶養者）に分割
- 各種ひな形CSV（BOM付きUTF-8）のダウンロード機能
- プレビュー機能（バリデーションエラー行を色分け表示）
- Shift-JIS/UTF-8自動判定
- ドラッグ&ドロップアップロード対応

---

## 現在残っている課題

1. **②③のインポート重複問題**
   - 同じCSVを2回インポートするとデータが重複登録される
   - 「全置換モード」（既存削除→再INSERT）の実装を検討中

2. **デバッグ用タイムアウト**
   - 現在admin.jsの更新後リダイレクトが10秒になっている
   - 動作確認後に1200msに戻す必要あり

---

## 環境情報

- WordPress設置場所：`/inhouse/`（URLから判断）
- テーブルプレフィックス：`wp_`
- プラグインディレクトリ：`wp-content/plugins/employee-manager/`
- デバッグログ：`wp-content/debug.log`（`[EMP]`プレフィックスで出力）
